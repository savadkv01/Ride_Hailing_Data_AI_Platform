select
    event_id,
    event_type,
    event_time,
    cast(event_time as date) as event_date,
    city_id,
    trip_id,
    rider_id,
    driver_id,
    vehicle_id,
    payment_id,
    refund_id,
    review_id,
    support_ticket_id,
    fraud_case_id,
    fare_total,
    surge_multiplier,
    promotion_amount,
    platform_fee,
    driver_payout,
    payment_amount,
    refund_amount,
    rating_value,
    fraud_score,
    latitude,
    longitude,
    avg_surge_multiplier,
    requested_trips,
    completed_trips,
    active_drivers,
    source_system,
    schema_version
from {{ source('staging', 'silver_canonical_events') }}
